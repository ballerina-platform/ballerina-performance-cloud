name: Relase Pack Test
on:
  workflow_dispatch:
    inputs:
      zipURL:
        description: 'Ballerina Zip Distribution URL'
        required: false
jobs:
  h1_h1_passthrough:
    name: h1_h1_passthrough
    runs-on: ubuntu-latest
    steps:
    - name: Trigger h1_h1_passthrough
      uses: felixp8/dispatch-and-wait@v0.1.0
      with:
        owner: anuruddhal
        repo: ballerina-performance-cloud
        token: ${{ secrets.BALLERINA_BOT_TOKEN }}
        event_type: ping
        client_payload: '{"zipURL":"http://dist-dev.ballerina.io/downloads/swan-lake-beta3/ballerina-swan-lake-beta3.zip"}'
        wait_time: 60
        max_time: 180